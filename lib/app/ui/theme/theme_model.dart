import 'package:flutter/material.dart';

class ThemeModel {
  String title;
  IconData icon;
  ThemeMode themeMode;

  ThemeModel({
    required this.title,
    required this.icon,
    required this.themeMode,
  });
}
